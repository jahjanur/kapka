{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2022"],
    /* The package ships browser-safe code, but its contrast test reads
       tokens.css off disk — so node types are needed to typecheck it. */
    "types": ["node"]
  },
  "include": ["src"]
}
