# dependencies
node_modules/
.pnp/
.pnp.js

# builds
dist/
build/
*.tsbuildinfo

# secrets — see §12 of the plan: a committed .env means rotating every key
.env
.env.*
!.env.example

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# test + tooling output
coverage/
playwright-report/
test-results/

# The embedded database and mailbox the end-to-end stack writes while it runs.
e2e/.tmp/
.vite/

# Rendered email fixtures — regenerate with npm run email:preview
apps/api/preview/

# Lighthouse CI reports — regenerated on every run.
.lighthouseci/
